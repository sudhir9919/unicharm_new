// const timer = setTimeout(() => {
    //   navigation.replace('Carousel'); // Replace with your target screen name
    // }, 1000);

    // Cleanup the timer on component unmount
    // return () => clearTimeout(timer);

  // }, [scaleAnim, navigation]);